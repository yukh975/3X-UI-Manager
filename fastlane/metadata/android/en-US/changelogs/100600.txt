Fixed: F-Droid builds verify again (#4), so updates flow to the catalog once more — this release also carries what 0.10.2-0.10.5 (test builds) fixed: clients created in the app now get a proper subscription ID instead of a raw UUID (#3), and the bottom navigation no longer breaks with an enlarged system font. Added: a Changelog page in About, and the app language now follows the device by default. Changed: the About button reads "Project on GitHub".
