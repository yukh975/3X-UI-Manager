# Built artifacts
*.apk
*.aab
*.aar
*.dex
*.class
build/
captures/

# IntelliJ / Android Studio
*.iml
.idea/
.gradle/
.kotlin/
local.properties

# Gradle wrapper jar — regenerated on demand (see README)
gradle/wrapper/gradle-wrapper.jar
gradlew
gradlew.bat

# Logs
*.log

# Keystores — never commit
*.jks
*.keystore
*.p12
*.pfx
keystore.properties
release.properties

# CI decode target for release keystore (outside this dir in CI, but belt+braces)
.release-keystore.*

# OS junk
.DS_Store
Thumbs.db

# internal / AI working files — kept out of the app repo
/docs/
/CLAUDE.md
/fdroid-publishing.md
